{
  "name": "node-red-contrib-mqtt-aviosys",
  "version": "1.0.1",
  "description": "MQTT broker that can resolve environment variables",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mqtt": "^2.18.8",
    "is-utf8": "^0.2.1"
  },  
  "author": "",
  "license": "ISC",
  "keywords": [ "node-red", "mqtt", "tls" ],
  "node-red" : {
        "nodes": {
            "mqtt-aviosys": "mqtt-aviosys.js"
        }
  }
}
